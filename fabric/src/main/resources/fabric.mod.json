{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "accessWidener": "chatpositioner.accesswidener",
  "name": "${mod_name}",
  "description": "${mod_desc}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {
    "sources": "${mod_sources}"
  },
  "license": "${mod_license}",
  "icon": "assets/chatpositioner/icon.png",
  "environment": "client",
  "entrypoints": {
    "main": [
      "space.plague.chatpositioner.fabric.MainFabric"
    ],
    "modmenu": [
      "space.plague.chatpositioner.fabric.config.gui.ModMenuIntegration"
    ]
  },
  "mixins": [
    "chatpositioner.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "minecraft": "~${minecraft_version}",
    "java": ">=${java_version}"
  },
  "suggests": {
    "modmenu": ">=${mod_menu_version}"
  }
}